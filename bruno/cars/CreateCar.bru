meta {
  name: CreateCar
  type: http
  seq: 3
}

post {
  url: http://localhost:3000/cars
  body: json
  auth: none
}

body:json {
  {
    "brand": "Ford",
    "model": "Mustang"
  }
}

script:post-response {
  if (res.status === 201) {
    bru.setVar("carId", res.body.id);
  }
}

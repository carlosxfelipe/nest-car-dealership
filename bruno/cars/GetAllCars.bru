meta {
  name: GetAllCars
  type: http
  seq: 1
}

get {
  url: http://localhost:3000/cars
  body: none
  auth: none
}

script:post-response {
  if (res.status === 200 && res.body.length > 0) {
    bru.setVar("carId", res.body[0].id);
  }
}
